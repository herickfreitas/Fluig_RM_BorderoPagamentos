function enableFields(form) {
	
	/*
	var atividade = parseInt(getValue("WKNumState"));
	if (atividade == 1) 
	{
		form.setEnabled("dt_validade", true);
		form.setEnabled("valortotal", true);
	}
	else 
	{
		form.setEnabled("dt_validade", false);
		form.setEnabled("valortotal", false);
	}
	
	*/

}